<h1>Generated List</h1>

<% if @recipe_foods.length > 0 %>
<h2>Amount of food items to buy: <%= @recipe_foods.length%></h2>

 <% total = 0 %>
 <% @recipe_foods.each do |food| %>
 <% total += food.food.price %>
 <% end %>
<p>Total value of food needed: $<%= total %> </p>

<table>
<tr>
<th>Food</th>
<th>Quantity</th>
<th> Price</th>
</tr>
<tbody>
<% @recipe_foods.each do |recipe|  %>
<tr>
<td><%= link_to "#{recipe.food.name}", food_path(recipe.food.id) %>(<%= recipe.food.quantity%>)</td>
<td><%= recipe.quantity%></td>
<td><%= recipe.food.price%></td>
</tr>
<% end %>
<tbody>
</table>

<% else %>
<h1>No shoping needed</h1>
<% end %>

